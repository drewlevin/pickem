'use strict';

angular.module('pickemApp')
  .controller('MainCtrl', function ($scope) {
    $scope.awesomeThings = [
      'Create and join leagues',
      'Pick winners vs. the spread',
      'Dominate your friends'
    ];
  });
